/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // i18n: {
  //   locales: ["hu"],
  //   defaultLocale: "hu",
  // }, //majd angol nyelvhez
};

module.exports = nextConfig;
